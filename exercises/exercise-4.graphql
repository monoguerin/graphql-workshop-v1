# Write a mutation Operation called 'CreateNote'
# This Operation will call 'createNote' mutation with generic arguments as an $input
# Get as response 'body' of the Note and Error 'message'

mutation CreateNote($input: CreateNoteInput!) {
  createNote(input: $input) {
    errors {
      message
    }
    note {
      body
    }
  }
}